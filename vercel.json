{
  "installCommand": "npm install",
  "buildCommand": "npm run build -w dro-web",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index.js" }
  ]
}
