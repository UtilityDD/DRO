{
  "installCommand": "npm install && npm install --prefix apps/web && npm install --prefix server",
  "buildCommand": "node scripts/vercel-build.js",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index.js" }
  ]
}
